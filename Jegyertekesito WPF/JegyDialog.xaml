<!-- JegyDialog.xaml -->
<Window x:Class="Jegyertekesito.JegyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Új jegy hozzáadása"
        Height="300"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Jegynév -->
        <TextBlock Text="Jegynév:"
                   Grid.Row="0"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <TextBox x:Name="NameTextBox"
                 Grid.Row="0"
                 Grid.Column="1"
                 Margin="0,0,0,10"
                 MaxLength="100"/>

        <!-- Ár (Ft) -->
        <TextBlock Text="Ár (Ft):"
                   Grid.Row="1"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <TextBox x:Name="PriceTextBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Margin="0,0,0,10"/>

        <!-- Darabszám -->
        <TextBlock Text="Darabszám:"
                   Grid.Row="2"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Margin="0,0,10,10"/>
        <TextBox x:Name="QuantityTextBox"
                 Grid.Row="2"
                 Grid.Column="1"
                 Margin="0,0,0,10"/>

        <!-- Gombok -->
        <StackPanel Grid.Row="4"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Content="Mentés"
                    Width="100"
                    Height="35"
                    Margin="5"
                    Click="SaveButton_Click"/>
            <Button Content="Mégsem"
                    Width="100"
                    Height="35"
                    Margin="5"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>